<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- App Theme -->
    <style name="Theme.ShalNammaPride" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color (teal) -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <!-- Secondary brand color (amber) -->
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorOnSecondary">@android:color/white</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <!-- Window background -->
        <item name="android:windowBackground">@color/windowBackground</item>
    </style>

    <!-- Splash Screen theme (shows immediately, no white flash) -->
    <style name="Theme.ShalNammaPride.Splash" parent="Theme.ShalNammaPride">
        <item name="android:windowBackground">@drawable/bg_splash_gradient</item>
        <item name="android:statusBarColor">#7C3AED</item>
    </style>

</resources>
