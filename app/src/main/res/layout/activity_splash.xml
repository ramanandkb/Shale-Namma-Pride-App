<?xml version="1.0" encoding="utf-8"?>
<!--
  activity_splash.xml - Splash screen layout
  Shows app logo (large emoji/icon), app name, and tagline.
  Background uses a colorful gradient.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_splash_gradient"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="32dp">

    <!-- App Logo (large school emoji / custom drawable) -->
    <ImageView
        android:id="@+id/imgLogo"
        android:layout_width="140dp"
        android:layout_height="140dp"
        android:layout_marginBottom="24dp"
        android:src="@drawable/ic_school_logo"
        android:contentDescription="App Logo" />

    <!-- App Name -->
    <TextView
        android:id="@+id/tvAppName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ಶಾಲೆ - Namma Pride"
        android:textColor="@android:color/white"
        android:textSize="30sp"
        android:textStyle="bold"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:layout_marginBottom="8dp" />

    <!-- Tagline -->
    <TextView
        android:id="@+id/tvTagline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Connecting Parents &amp; Schools 🌟"
        android:textColor="#E0FFFFFF"
        android:textSize="16sp"
        android:gravity="center"
        android:fontFamily="sans-serif" />

</LinearLayout>
