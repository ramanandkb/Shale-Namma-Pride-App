<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShalNammaPride"
        tools:targetApi="31">

        <!-- Splash Screen (launched first) -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.ShalNammaPride.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Screen -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Home Dashboard -->
        <activity
            android:name=".activities.HomeActivity"
            android:exported="false" />

        <!-- Daily Meal Screen -->
        <activity
            android:name=".activities.MealActivity"
            android:exported="false" />

        <!-- Facilities Screen -->
        <activity
            android:name=".activities.FacilitiesActivity"
            android:exported="false" />

        <!-- Student Stars Screen -->
        <activity
            android:name=".activities.StudentStarsActivity"
            android:exported="false" />

        <!-- Feedback Screen -->
        <activity
            android:name=".activities.FeedbackActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Admin Upload Meal Screen -->
        <activity
            android:name=".activities.AdminUploadMealActivity"
            android:exported="false" />

        <!-- Admin Add Student Star Screen -->
        <activity
            android:name=".activities.AdminAddStudentActivity"
            android:exported="false" />

    </application>

</manifest>
